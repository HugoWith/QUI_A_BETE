function displayBet {
  const betwip = document.querySelectorAll('#bet_in_progress');
  console.log(betwip)
  betwip.addEventListener('click', () => {
    console.log(coucou);
  });
  // betwip.innerHTML = innerHTML;
}

// displayBet('<%= j render "groups/form", restaurant: @restaurant, group: @group %>');



// function addGroup(groupHTML) {
//   const groups = document.getElementById('groups');
//   groups.insertAdjacentHTML('beforeend', groupHTML);
// }



