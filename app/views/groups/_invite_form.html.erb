<%= simple_form_for([@group, @invitation]) do |f| %>
  <%= f.simple_fields_for :invited_users do |form| %>
    <%= form.input :email, label: "📩  Email" %>
  <% end %>
  <%= f.button :submit, class: "btn-home", value: "In-crew-ste les" %>
<% end %>
